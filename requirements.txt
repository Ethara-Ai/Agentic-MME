# Core dependencies
openai>=1.46.1
pillow>=10.0.0
numpy>=1.24.0
matplotlib>=3.8.0
requests>=2.31.0
litellm>=1.40.0
boto3>=1.28.0

# Optional: torch, transformers, accelerate (for local model runners: thyme/deepeyes)
# Optional: opencv-python (for denoise/edge_detect — gracefully degrades to PIL)
